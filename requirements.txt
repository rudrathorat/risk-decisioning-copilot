# Core (required for run.py and demo_copilot.py)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Optional: better explanations in copilot reports (code works without it)
shap>=0.42.0

# Optional: extra model choice (code uses logistic/forest/GB if XGBoost missing)
xgboost>=2.0.0

# Streamlit UI for AI Underwriting Copilot (app.py)
streamlit>=1.28.0
